export const serviceHost = "localhost";

export const servicePort = "7002";

export const openUrlPrefix = "/oapi/v1";

export const privateUrlPrefix = "/api/v1";

export const protocal = "http";

export const urlMainOpen = `${protocal}://${serviceHost}:${servicePort}${openUrlPrefix}`;

export const urlMainPrivate = `${protocal}://${serviceHost}:${servicePort}${privateUrlPrefix}`;
